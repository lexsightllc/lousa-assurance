#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

"${ROOT_DIR}/scripts/lint" "$@"
"${ROOT_DIR}/scripts/typecheck" "$@"
"${ROOT_DIR}/scripts/test" "$@"
"${ROOT_DIR}/scripts/coverage" "$@"
"${ROOT_DIR}/scripts/security-scan" "$@"

#!/usr/bin/env bash
# SPDX-License-Identifier: MPL-2.0
set -euo pipefail

source "$(dirname "$0")/common.sh"

install_dev_dependencies

if has_py_files "${ROOT_DIR}/tests/e2e"; then
    "${VENV_PATH}/bin/pytest" tests/e2e "$@"
else
    echo "No end-to-end tests defined. Skipping." >&2
fi

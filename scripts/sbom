#!/usr/bin/env bash
# SPDX-License-Identifier: MPL-2.0
set -euo pipefail

source "$(dirname "$0")/common.sh"

install_dev_dependencies
mkdir -p "${ROOT_DIR}/sbom"
"${VENV_PATH}/bin/cyclonedx-bom" -o "${ROOT_DIR}/sbom/cyclonedx.json" -F json
